pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                echo "Branch: ${env.BRANCH_NAME}"
                echo 'Pipeline is running.'
            }
        }
    }
}
